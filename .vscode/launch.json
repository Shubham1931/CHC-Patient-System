{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch CHC Patient Management",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/start.js",
      "outFiles": ["${workspaceFolder}/**/*.js"],
      "internalConsoleOptions": "openOnSessionStart"
    }
  ]
}